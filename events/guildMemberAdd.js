const moment = require('moment')
const Discord = require('discord.js')

module.exports = async (client, member) => {
    
    console.log('alou')
    
    member.guild.channels.get(config.JOINCHANNEL).send(join).catch()
  }